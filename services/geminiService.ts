// Gemini AI service - disabled for multiplayer mode
// Kept as placeholder for potential bot practice mode in the future

export const generateBotChat = async (
  _botName: string,
  _context: string,
  _personality: string = "aggressive"
): Promise<string> => {
  return "...";
};
