# Production Environment Variables
# Since both client and server are on the same Render deployment,
# we can use window.location.origin to connect to the same domain

# This will be automatically set to your Render URL (e.g., https://pokerpov.onrender.com)
# Leave empty to use the same origin as the client
VITE_SERVER_URL=
