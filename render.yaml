services:
  - type: web
    name: pokerpov
    runtime: node
    plan: free
    buildCommand: npm install && npm run build:all
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: development
      - key: PORT
        value: 10000
    healthCheckPath: /health